# `pnpm run dev:live` / `vite --mode live` 加载本文件：直连线上 bns-server 浏览真实数据。
#
# VITE_BNS_SERVER_URL     bns-server 基地址（kRPC 在 /kapi/bns，DID Resolver 在 /1.0/identifiers/）
# VITE_BNS_CHAIN_ID       部署期望 chain id，与 system.info 二次比对（PRD 8.1）
# VITE_BNS_CONTRACT_ADDRESS
#     构建期钉死的 BNS Proxy 地址（contractTrust=pinned 的比对锚点）。
#     运行时地址仍来自 system.info，钉死值只用于发现「连错了服务 / 服务被换链」并阻断写入。
#     2026-08-05 由 bns.buckyos.ai 运维确认，并通过 OP Mainnet RPC 验证 Proxy 已部署。
VITE_BNS_SERVER_URL=https://bns.buckyos.ai
VITE_BNS_CHAIN_ID=10
VITE_BNS_CONTRACT_ADDRESS=0x68aD9f8f551e2f9115B6b38d3D4CA02A847c43CC
