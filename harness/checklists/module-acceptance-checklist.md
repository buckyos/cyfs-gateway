# 模块验收检查清单

- 确认模块包存在，且必需文件齐全。
- 确认 proposal、design、testing、acceptance 与长期模块文档在范围和归属上保持一致。
- 确认 `testplan.yaml` 与 `testing.md` 中声明的可执行命令一致。
- 确认所有已触发的高风险面都已审查并验证。
- 确认跳过项或高成本检查已被显式记录。
- 确认验收输出写入独立报告，而不是直接写回 `acceptance.md`。
